---
const techLogos = [
  {
    name: "OpenAI",
  },
  {
    name: "Anthropic",
  },
  {
    name: "DeepSeek",
  },
  {
    name: "Huggingface",
  },
  {
    name: "Ollama",
  },
  {
    name: "Replicate",
  }
];
---

<div class="py-24 md:py-32 bg-off-white">
  <div class="container mx-auto px-6 md:px-8">
    <p class="text-sm text-dark-gray/60 mb-12 text-center">Expertise across leading AI platforms and frontier labs</p>
    <div class="grid grid-cols-2 md:grid-cols-3 gap-12 items-center justify-items-center max-w-3xl mx-auto">
      {techLogos.map(tech => (
        <div class="group relative flex items-center justify-center">
          <img 
            src={`/logos/${tech.name.toLowerCase()}-2.svg`}
            alt={`${tech.name} Logo`}
            class="h-6 md:h-8 w-auto opacity-50 group-hover:opacity-100 transition-opacity duration-300"
          />
          <span class="absolute -bottom-6 text-xs text-dark-gray/60 opacity-0 group-hover:opacity-100 transition-opacity duration-300">
            {tech.name}
          </span>
        </div>
      ))}
    </div>
  </div>
</div>