---
const techLogos = [
  {
    name: "OpenAI",
  },
  {
    name: "Anthropic",
  },
  {
    name: "DeepSeek",
  },
  {
    name: "Huggingface",
  },
  {
    name: "Ollama",
  },
  {
    name: "Replicate",
  }
];
---

<div class="py-24 md:py-30 bg-off-white">
  <div class="container mx-auto px-6 md:px-8">
    <p class="text-base md:text-lg text-dark-gray/80 mb-16 text-center">Expertise across leading AI platforms and frontier labs</p>
    <div class="grid grid-cols-2 md:grid-cols-3 gap-12 md:gap-16 items-center justify-items-center max-w-4xl mx-auto">
      {techLogos.map(tech => (
        <div class="group relative flex items-center justify-center">
          <img 
            src={`/logos/${tech.name.toLowerCase()}-2.svg`}
            alt={`${tech.name} Logo`}
            class="h-8 md:h-12 w-auto opacity-40 group-hover:opacity-100 transition-opacity duration-300"
          />
          <span class="absolute -bottom-6 text-xs text-dark-gray/60 opacity-0 group-hover:opacity-100 transition-opacity duration-300">
            {tech.name}
          </span>
        </div>
      ))}
    </div>
  </div>
</div>